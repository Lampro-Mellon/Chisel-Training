when(/*condition for when*/)
{/*do this when the upper when is true*/}
.elsewhen(/*condition for elsewhen*/)
{/*do this when the upper when is false and 
  condition of .elsewhen is true*/}
.otherwise
{/*do this as a default condition, not .otherwise
  do not require a condition as it is the default
  if no condition is meet*/}
