switch (state) {
  is(state1){
    // logic implementation which runs when state === state1
  }
  is(state2){
    // logic implementation which runs when state === state1
  }
    // what about the default state
}